export type WithingsDevice = {
  mac_address?: string;
  type: string;
  model: string;
  model_id: number;
  battery: string;
  deviceid: string;
  hash_deviceid?: string;
  timezone: string;
  first_session_date?: string;
  last_session_date: string;
};
